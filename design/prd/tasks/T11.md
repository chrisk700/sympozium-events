# T11 — Tailscale annotation injection

T04 result: Option 1 works — annotations injected via top-level `spec.service.metadata.annotations` on the EventSource.

## Sub-items

- [x] Add `tailscale.enabled` conditional to EventSource template — when true, populate top-level `spec.service.metadata.annotations` from the `tailscale.annotations` map in values
- [x] Set `spec.service.ports` to match the webhook port when Tailscale is enabled
- [x] `helm template --set tailscale.enabled=true` — verify annotations appear on the rendered `EventSource`
