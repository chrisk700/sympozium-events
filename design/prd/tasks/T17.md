# T17 — Smoke test: BYO Argo Events

Validate BYO mode (`argoEvents.install: false`) with an existing Argo Events installation.

## Sub-items

- [x] Uninstall the full chart; leave Argo Events running in the cluster
- [x] `helm install sympozium-events ./sympozium-events -f test-values.yaml --set argoEvents.install=false`
- [x] Confirm `EventSource` and `Sensor` CRs created; Argo Events controller `Deployment` is untouched
- [x] Repeat T15 POST test — confirm identical behaviour in BYO mode
