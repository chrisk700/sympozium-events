# T06 — EventSource template

`templates/eventsource.yaml` — generate `EventSource` CRs from `routes[].sources`.

## Sub-items

- [x] Iterate `routes`, iterate `sources` per route, emit one `EventSource` with one entry per source
- [x] Support day-one source types: `webhook`, `sqs`, `sns`, `calendar`, `resource` (K8s events), `nats`
- [x] Wire `eventBusName` to the chart-managed EventBus (T02 result: reuse always false; Argo Events runs its own JetStream)
- [x] Include Tailscale annotation injection hook — top-level `spec.service.metadata.annotations` populated in T11; leave as no-op placeholder here
- [x] `helm template` against an example values file with two routes of different source types; verify the output is a valid `EventSource` spec
