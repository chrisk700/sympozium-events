# T14 — Per-sender HMAC auth

Inject per-sender token validation into EventSource webhook specs via the Argo Events `webhook.secret` field.

## Sub-items

- [x] `auth.tokenSecret`: name of a Kubernetes Secret containing per-sender HMAC tokens; wire into EventSource webhook source via `spec.webhook.<name>.secret.name`
- [x] Inject into EventSource template — only for `webhook` source type, gated on `auth.tokenSecret` being non-empty
- [x] Add values comment: token rotation requires only Secret update, no chart redeploy
- [x] Live test: POST without token → rejected (4xx); POST with correct token → event fires and `AgentRun` is created
