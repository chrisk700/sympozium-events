# T01 — Init repository and chart scaffold

All code lives in a new standalone `sympozium-events` repository (private, GitHub).

## Sub-items

- [x] `newrepo sympozium-events` — creates private GitHub repo, clones it, adds README
- [x] `cd sympozium-events && helm create sympozium-events` — then strip the example templates (remove `templates/deployment.yaml`, `hpa.yaml`, `ingress.yaml`, `service.yaml`, `serviceaccount.yaml`, `tests/`)
- [x] `Chart.yaml`: set `name: sympozium-events`, add `argo-events` as a conditional Helm dependency
- [x] Stub `values.yaml` with top-level keys: `argoEvents`, `namespace`, `eventbus`, `tailscale`, `auth`, `serviceAccount`, `conditionWindow`, `routes`
- [x] Confirm `helm lint` passes on the empty chart
