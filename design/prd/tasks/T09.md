# T09 — NetworkPolicy template

`templates/networkpolicy.yaml`

## Sub-items

- [x] Allow egress from EventSource/Sensor pods in `argo-events` namespace to the Argo Events JetStream NATS on port 4222 within `argo-events`
- [x] Gate on `networkPolicy.enabled` value (default `true`)
- [x] Verify Sympozium's existing deny-all NetworkPolicy (scoped to `sympozium.ai/role: agent` pods) does not catch Argo Events pods — confirm by label inspection (`kubectl get networkpolicy -n sympozium-system -o yaml`)
