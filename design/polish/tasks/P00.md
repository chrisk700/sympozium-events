# P00 — Retrospective: close cross-document gaps from T01–T17

The `/next-task` command now requires a cross-document impact check at completion (Step 6). T01–T17 were completed before this rule existed. This task does the retrospective pass that those tasks skipped: finding findings, decisions, and design changes that were recorded in task files but never propagated back to the PRD, implementation notes, or related tasks.

P01 covers the major known gaps (open questions, stale design text). This task finds the ones P01 missed by doing a systematic read of every completed task file.

## Sub-items

- [x] Read T02–T17 task files in full. For each, check:
  - Does the task's `## Findings` or `## Results` section contradict or update anything in `docs/prd/prd.md`?
  - Does it resolve a PRD open question that isn't yet marked closed?
  - Does it record a design decision (explicit or implicit) not in the PRD Decisions section?
  - Does it introduce a known limitation not in the PRD or `docs/polish/polish.md`?

- [x] Compile a list of gaps found. For each gap: which task, which document, what change is needed.

- [x] Apply the changes — update `docs/prd/prd.md` and `docs/polish/polish.md` directly. Do not create new files; fold findings into existing sections (Implementation Notes, Decisions, Known Limitations, Open Questions).

- [x] For each task file that had a gap: add a `## Cross-document updates` section recording what was propagated and where.

- [x] Confirm `docs/prd/prd.md` Implementation Notes section is consistent with the final chart implementation (no references to design approaches that were superseded, e.g. `reuseSympoziumEventbus`, the original `conditionWindow` wording).

## Known gaps to start from (found during polish planning)

These are already known before reading the task files — use as a starting checklist, not an exhaustive list:

| Task | Finding | Target document | Status |
|------|---------|-----------------|--------|
| T02 | EventBus reuse fails; Sympozium NATS is named `nats` not `eventbus-default-js` | PRD arch diagram, Implementation Notes | Pending P01 |
| T04 | Option 1 works; Options 2/3 not needed | PRD Security section (still describes all 3 as open) | Pending P01 |
| T15 | sessionKey collision on duplicate POSTs; Argo Events dedup is header-based not content-based | PRD open question OQ-4, Implementation Notes | Pending P01 |
| T16 | `conditionWindow` is not a native Argo Events field; implemented as `conditionsResetCron`; PRD `conditionWindow` key and `conditionsReset` reliability gap | PRD Open Questions OQ-2, Implementation Notes, Key Values example | Pending P01 |
| T16 | Two webhooks on same port with distinct endpoints work — one server, routed by path | PRD (not mentioned) | May need a note |
| T12 | T12 was skipped (Option 2 Job not needed per T04 result) — but task is marked `[x]` | tasks.md, T12.md | Check: is skip documented? |

## Cross-document updates

**`docs/prd/prd.md`** — 20 gaps applied across the document:

- **Status**: Updated from "Planned" to "Implementation complete (T01–T17); polish in progress".
- **Architecture diagram**: EventBus label corrected to Argo Events' own dedicated JetStream.
- **Why Argo Events**: NATS reuse paragraph updated from open question to confirmed finding (reuse fails; T02).
- **Packaging**: `eventbus.yaml` comment updated; `reuseSympoziumEventbus` removed.
- **Key Values**: Removed `eventbus.reuseSympoziumEventbus` and `eventbus.url`; `conditionWindow` → `conditionsResetCron`.
- **Route example**: `conditionWindow: 60s` → `conditionsResetCron: "*/30 * * * *"` with corrected semantics.
- **Routing Model description**: Updated to describe `conditionsResetCron` and its cron/wall-clock approximation.
- **Security section**: Updated from future-tense planning to past-tense outcome — Option 1 chosen; Options 2/3 status clarified.
- **Workstreams**: Reuse fallback paragraph updated; `reuseSympoziumEventbus` reference removed.
- **Implementation Notes** (conditionWindow scope): Updated to "conditionsReset" with actual findings.
- **Implementation Notes** (version floor): Updated to confirmed fact (≥ 1.7; v1.9.10 tested).
- **Implementation Notes** (new — sessionKey uniqueness): Content-based dedup limitation added.
- **Implementation Notes** (new — multi-source port routing): T16 finding added.
- **Alternatives table**: Argo Events "NATS EventBus reuse" updated from ✅ Configurable to ❌.
- **Decisions**: `conditionWindow` default updated to `conditionsResetCron`.
- **`sessionKey` generated YAML note**: Corrected "must be unique" to clarify Sympozium does not enforce it.
- **Open Questions**: Restructured into Closed/Open — 4 questions marked closed (EventBus reuse T02, conditionWindow scope T16, version floor T03, sessionKey uniqueness T03/T15); 2 remain open (cleanup policy P10, native CRD path PRD-08).

**`docs/polish/polish.md`** — 1 addition:

- **What Was Learned**: Added T16 multi-webhook port routing finding.

**Task files** — `## Cross-document updates` sections added to: T02, T03, T04, T07, T08, T15, T16.
