# T08 — EventBus template

`templates/eventbus.yaml`

T02 result: reuse always fails — Argo Events must run its own JetStream StatefulSet.

## Sub-items

- [x] Emit a standard Argo Events JetStream `EventBus` CR in `argoEvents.resourceNamespace`
- [x] ~~Gate on `eventbus.reuseSympoziumEventbus`~~ — dropped; T02 confirmed reuse always fails, CR is always emitted
- [x] `helm template` — verify a `EventBus` CR is rendered with default values

## Cross-document updates

Propagated to `docs/prd/prd.md` (P00 retrospective, jointly with T02):

- **Packaging**: `eventbus.yaml` comment updated to "always emitted; Argo Events manages its own JetStream".
- **Key Values**: Removed `eventbus.reuseSympoziumEventbus` and `eventbus.url` fields; replaced with a comment explaining the EventBus always emits its own CR.
