# T12 — Post-install Job (Tailscale fallback)

T04 result: Option 1 (annotation passthrough) works. This task is a no-op for v1 — the Job template is not needed.

`templates/tailscale-patch-job.yaml`

## Sub-items

- [x] Gate template on `tailscale.enabled: true` AND `tailscale.annotationMode: job` — emit nothing under the default `annotationMode: annotation`
- [x] Job script: `kubectl wait --for=jsonpath='{.status.loadBalancer.ingress}' svc/<name> --timeout=120s`, then `kubectl annotate svc/<name> <tailscale-annotations>`
- [x] Document the kubectl image choice in values comments (minimal, no extra dependencies)
- [x] `helm template --set tailscale.enabled=true --set tailscale.annotationMode=job` — confirm Job is rendered; `helm template` with defaults — confirm nothing is rendered
