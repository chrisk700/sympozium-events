# T05 — RBAC template

`templates/rbac.yaml`

## Sub-items

- [x] `ServiceAccount` in `argoEvents.resourceNamespace` — gated on `serviceAccount.create: true`
- [x] `Role` in `sympozium.namespace` — `create` on `agentruns` in `sympozium.ai` apiGroup
- [x] `RoleBinding` in `sympozium.namespace` binding the SA from `argo-events` namespace (cross-namespace RoleBinding — subject namespace set explicitly)
- [x] When `serviceAccount.create: false`: SA skipped; Role + RoleBinding still rendered, binding `serviceAccount.name`
- [x] `helm template` verified — RoleBinding subject namespace matches `argoEvents.resourceNamespace` in both `create: true` and `create: false` paths

## Results

Template at `templates/rbac.yaml`. Key design:

- SA gated on `serviceAccount.create: true`, name from `sympozium-events.serviceAccountName` helper
- Role name: `<fullname>-agentrun-creator` in `.Values.namespace` (sympozium-system)
- RoleBinding in `.Values.namespace`; subject `namespace: {{ .Values.argoEvents.resourceNamespace }}` — the critical cross-namespace field
- `create: false` + `serviceAccount.name: my-sa`: SA omitted, RoleBinding binds `my-sa` in `argoEvents.resourceNamespace`
