# T13 — BYO Argo Events mode

`argoEvents.install: false` — use an existing Argo Events installation; skip the Helm sub-chart.

## Sub-items

- [x] `argoEvents.install: false` skips the `argo-events` Helm dependency via `condition: argoEvents.install` in `Chart.yaml` (already set in T01)
- [x] Add values comment: `installNamespace` is ignored in BYO mode
- [x] Confirm `resourceNamespace` controls where `EventSource`/`Sensor` CRs are created in both install and BYO modes
- [x] Confirm `serviceAccount.create: false` + `serviceAccount.name` path works in BYO mode — Role + RoleBinding only
- [x] `helm template --set argoEvents.install=false` — verify no Argo Events controller Deployment, ClusterRole, or CRD pre-install hooks are rendered
